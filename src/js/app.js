/* eslint-disable no-console */
// TODO: write code here
// document.getElementsByTagName('img')[0].setAttribute('alt', 'img/netology.svg');

export default function foIch() {
  let summ = 0;
  [1, 2, 3].forEach((elem) => {
    summ += elem;
  });
  return summ;
}
